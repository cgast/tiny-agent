# API Keys for LLM providers
# Copy this file to .env and add your actual keys

# OpenAI
OPENAI_API_KEY=sk-your-openai-key-here

# Anthropic
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Other providers (if needed)
# TOGETHER_API_KEY=your-together-key
# GROQ_API_KEY=your-groq-key

# Agent Configuration (optional - defaults in config.yaml)
# Uncomment and modify to override config.yaml values

# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4
# MAX_ITERATIONS=10
# COMMAND_TIMEOUT=30
# MAX_RETRIES=3
# MAX_OUTPUT_SIZE=5000
# LOG_LEVEL=INFO

# Auto-detect CLI Commands
# Enable to automatically detect available CLI tools (ls, cat, grep, etc.)
# Recommended for Docker/sandbox environments where tools are pre-installed
# AUTO_DETECT_CLI=true

# Optional: Restrict to specific commands (comma-separated)
# CLI_ALLOWLIST=ls,cat,grep,find,jq

# Optional: Block specific commands (comma-separated, merged with defaults)
# Default blocklist: rm, sudo, chmod, chown, kill, reboot, shutdown, etc.
# CLI_BLOCKLIST=wget,curl
